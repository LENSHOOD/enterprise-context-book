# 《企业上下文》第五轮同行评审：对 RC2 处置批次（2026-08-28）的验收

评审日期：2026-08-28
评审对象：12:48–12:51 落盘的 20 个文件改动 + `reviews/PEER_REVIEW_RC2_DISPOSITION_2026-08-28.md`
评审方式：与第四轮相同，处置声明逐项对照仓库实测，不采信自述。

**判定：本批次六项处置全部验证属实，R4-1 至 R4-6 正式闭环。发布裁决维持 `not-cleared`（与 `run_manifest.json` 一致），剩余阻断项即处置记录所列六项，与我的独立核算一致。本轮无新增 P0/P1 问题，仅一条 P2 微瑕。**

---

## 1. 逐项验证结果

| 条目 | 处置声明 | 实测 | 结论 |
| --- | --- | --- | --- |
| R4-1 | 双实现有意保留，文件头互指，处置勘误为"部分接受" | `context_demo.py` 与 `northstar.py` 头部 docstring 均说明重复目的并互相指认，写明"生产代码应共享实现"；处置记录"勘误"节明确改判 P1-14 为部分接受 | **通过**。选了我推荐的 (b) 路线，且勘误方式正确——不回写旧记录、向前更正 |
| R4-2 | §12.7 保留主讲，第 2、10、17 章压缩 | 三处均已压为一句边界结论 + 「详见第 12 章 §12.7」（ch02:59、ch10:105、ch17:77） | **通过** |
| R4-3 | 双时间语义修复 + 三条新测试 | 17 个对象逐一核验 `valid_from ≤ observed_at` 且日期版本对齐，**0 违例**，`valid_from` 分布在 10 个真实业务日期上；新增 `test_valid_time_precedes_system_observation_time`、`test_date_versions_start_on_their_business_date`、`test_bitemporal_query_selects_policy_valid_at_that_time`（三态：变更前 / 变更后 / 业务已生效但系统未摄取）；`documents_as_of` 经 `visible_documents(principal)` 走 ACL；ch11:108 把它接入正文并如实声明"只保存首次观察时间，精确重放仍需当时的 Manifest" | **通过，超出要求**。我的加分项（把 ch11 招牌例子变成可运行断言）也做了，且限界声明诚实 |
| R4-4 | 字数门拆独立 job | `docs.yml` 现为 `test` / `release_gate`（字数门）/ `build (needs: test)` / `deploy (needs: build)` 四 job；构建与 RC 预览不再被 8 万字门短路，正式发布仍要求 `release_gate` 转绿 | **通过** |
| R4-5 | 统一章末标题、补三章延伸阅读 | 「本章小结」18/18；ch02、ch03、ch14 延伸阅读均已补 | **通过** |
| R4-6 | blocking_gates 补第六项、审计写发布路径 | `run_manifest.json` 已含 `version_control_integration`；`RELEASE_AUDIT.md` 写明两条发布路线、索引清理要求（`node_modules`/`dist`/`__pycache__` 排除、`package-lock.json` 纳入）及关闭条件 | **通过**。不执行建仓/推送与授权边界的判断继续成立 |

**回归**：测试 30 + 10 = 40 全绿（前轮 37）；字数 56,348（+84，缺口 23,652）；`dist/` 无 `language-mermaid`；`context_demo.py`、`action_demo.py` 命令未破坏；`examples/enterprise-case/README.md` 同步更新了双时间字段的含义说明。

## 2. 本轮唯一新意见

### R5-1（P2）｜08-28 的改动被并入日期为 08-27 的 rc.2 条目

**定位**：`CHANGELOG.md:3-10`
**证据**：本批次两条新 bullet（双时间修正、CI 拆分）追加在「1.0.0-rc.2 — 2026-08-27」条目下，但改动实际发生于 08-28。对一本把"版本与勘误"写成附录 G 的书，版本记录的日期语义应当准确。
**修改动作**（任选其一，一分钟工作量）：把条目日期改为「2026-08-27/28」；或新起「1.0.0-rc.2.1 — 2026-08-28」小节收纳这两条。

## 3. 结语

四轮评审加两轮处置，这个仓库完成了一次罕见的完整闭环：评审指出问题 → 处置逐条表态（接受/部分/拒绝各有理由）→ 修订可被命令复现 → 处置自身的粒度缺陷也被下一轮勘误。第四轮我说"说的和做的不一致这个病基本痊愈,只剩余症"——本轮余症也清了。现在仓库里没有任何一条我已知的、声明与事实不符的陈述。

剩余六个阻断项全部是**做出来**而不是**改正确**的工作：约 23,652 字的正文（主料是把已验证的代码转写为第四部分）、集中参考文献与声明级引用审计、浏览器 QA、全内核原始证据、远端 Pages、版本控制接入。其中前两项是写作量，后四项是发布动作。质量流程已经不再是这本书的短板。

—— 第五轮评审完毕。R4-1 至 R4-6 全部闭环；新增 R5-1（P2）一条；`not-cleared` 维持，等待六个阻断项。
